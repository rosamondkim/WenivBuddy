# 질문

1. 질문 제목에 이미지를 붙여 넣으면 아래와 같이 나와
   제목 :`-` 본문:OCR로 추출한 텍스트, 이미지, 답변, 이미지

이건 답변을 등록할때, 질문을 이미지 자체로 넣었을 때 생기는 문제야.

1. 이미지 중복 표시를 해결해줘, 답변 하단에 이미지가 나오지 않게 해야해
2. 제목이 :`-` 형태로 나오지 않고 `node : 'node' 용어가 cmdlet, 함수, 스크립트 파일 또는 실행할 수 있는 프로그램 이름으로 인식되지 않습니다` 처럼 텍스트 형태로 나오게 해줘

# 답변
