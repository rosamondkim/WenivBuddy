# 질문

현재 Q&A 데이터베이스에 질문 리스트가 올라가게 될텐데,분야별 (FE,BE 등)로 데이터가 많이 쌓이면 체크하기 어려워질거야. 카테고리별로 나눠줘

# 답변

## ✅ 완료: Q&A 데이터베이스 카테고리별 분리

Q&A 데이터가 많아질 때를 대비하여 카테고리별로 데이터를 분리하고 관리하기 쉽게 구조화했습니다.

---

## 📁 새로운 폴더 구조

```
public/data/
├── qna-database.json          # 전체 Q&A 데이터 (기존 통합본)
└── categories/                # ✨ NEW: 카테고리별 분리 데이터
    ├── README.md              # 사용 가이드
    ├── stats.json             # 카테고리별 통계
    ├── frontend.json          # Frontend 카테고리 (1개)
    ├── backend.json           # Backend 카테고리 (1개)
    ├── css.json               # CSS 카테고리 (2개)
    ├── javascript.json        # JavaScript 카테고리 (1개)
    ├── git.json               # Git 카테고리 (1개)
    └── tools.json             # 도구 카테고리 (2개)
```

---

## 🎯 해결한 문제점

### Before (이전)
```
❌ 모든 Q&A가 하나의 파일에 섞여 있음
❌ 특정 분야만 확인하기 어려움
❌ 데이터가 많아지면 파일이 너무 커짐
❌ 협업 시 merge conflict 발생 위험
```

### After (개선)
```
✅ 카테고리별로 독립적인 파일 관리
✅ 원하는 분야만 빠르게 확인 가능
✅ 파일 크기를 적절하게 유지
✅ 각 분야별 전문가가 독립적으로 관리 가능
✅ stats.json으로 한눈에 현황 파악
```

---

## 📊 현재 카테고리별 통계

| 카테고리 | Q&A 수 | 총 조회수 | 파일명 |
|---------|--------|----------|--------|
| CSS | 2개 | 290회 | css.json |
| 도구 (VSCode 등) | 2개 | 120회 | tools.json |
| Backend | 1개 | 121회 | backend.json |
| JavaScript | 1개 | 93회 | javascript.json |
| Frontend | 1개 | 87회 | frontend.json |
| Git | 1개 | 67회 | git.json |
| **전체** | **8개** | **778회** | - |

---

## 💡 주요 기능

### 1. 카테고리별 파일 분리
각 카테고리가 독립적인 JSON 파일로 관리됩니다.

**예시: css.json**
```json
{
  "category": "CSS",
  "qnaList": [
    {
      "id": "qna-003",
      "category": "CSS",
      "question": "flexbox에서 justify-content와 align-items 차이가 헷갈려요",
      "keywords": ["CSS", "flexbox", "justify-content", "align-items"],
      "answer": "...",
      "views": 156
    },
    {
      "id": "qna-008",
      "category": "CSS",
      "question": "position: absolute를 사용하면 요소가 사라져요",
      "keywords": ["CSS", "position", "absolute"],
      "answer": "...",
      "views": 134
    }
  ]
}
```

### 2. 실시간 통계 (stats.json)
카테고리별 Q&A 개수, 조회수, 최근 업데이트 날짜를 자동으로 집계합니다.

```json
{
  "lastUpdated": "2024-12-04T09:15:00",
  "totalQnA": 8,
  "totalViews": 778,
  "categories": {
    "CSS": {
      "count": 2,
      "views": 290,
      "latestUpdate": "2024-12-04T09:15:00"
    },
    "Frontend": {
      "count": 1,
      "views": 87,
      "latestUpdate": "2024-12-02T14:15:00"
    }
  },
  "topCategories": [
    { "name": "CSS", "count": 2, "views": 290 },
    { "name": "도구", "count": 2, "views": 120 }
  ]
}
```

### 3. 상세한 사용 가이드 (README.md)
`public/data/categories/README.md`에 다음 내용이 포함되어 있습니다:

- 새로운 Q&A 추가 방법 (4단계)
- 데이터 구조 설명
- 카테고리 정의 및 분류 기준
- 향후 자동화 계획

---

## ✏️ 새로운 Q&A 추가 방법

### 1단계: 카테고리 선택
해당하는 카테고리 파일을 선택하세요:
- Frontend 관련 → `frontend.json`
- Backend 관련 → `backend.json`
- CSS 관련 → `css.json`
- JavaScript 관련 → `javascript.json`
- Git 관련 → `git.json`
- VSCode, 도구 관련 → `tools.json`

### 2단계: 카테고리 파일에 데이터 추가
해당 카테고리 파일의 `qnaList` 배열에 새로운 객체를 추가하세요.

```json
{
  "id": "qna-009",
  "category": "Frontend",
  "question": "질문 내용",
  "keywords": ["키워드1", "키워드2", "키워드3"],
  "answer": "답변 내용",
  "author": "작성자 이름",
  "timestamp": "2024-12-04T10:00:00",
  "imageUrl": "/images/example.png",
  "views": 0
}
```

### 3단계: 전체 데이터베이스 업데이트
`qna-database.json` 파일에도 동일한 데이터를 추가하세요.
(나중에 자동화 스크립트로 대체 예정)

### 4단계: 통계 업데이트
`stats.json` 파일의 해당 카테고리 통계를 업데이트하세요:
- `count`: Q&A 개수 +1
- `views`: 조회수는 초기값 0
- `latestUpdate`: 현재 날짜로 변경

---

## 🚀 향후 개선 계획

### Phase 1: 자동화 스크립트 (1주일 내)
```bash
# 카테고리별 파일을 자동으로 하나로 합치기
npm run merge-qna

# 통계 자동 생성 및 업데이트
npm run generate-stats

# 새 Q&A 추가 CLI 도구
npm run add-qna -- --category=Frontend --question="질문" --answer="답변"
```

### Phase 2: 검증 시스템 (2주일 내)
- 중복 ID 자동 체크
- 필수 필드 검증 (question, answer, keywords 등)
- 키워드 일관성 검사 (대소문자, 띄어쓰기)
- 카테고리 유효성 검증

### Phase 3: 선택적 로딩 (1개월 내)
현재는 전체 데이터를 로드하지만, 필요한 카테고리만 로드하도록 개선:

```javascript
// lib/qna-loader.js (향후 구현)

// 특정 카테고리만 로드
const frontendQnA = await loadQnAByCategory('Frontend');

// 여러 카테고리 동시 로드
const techQnA = await loadQnAByCategories(['Frontend', 'Backend', 'JavaScript']);

// 전체 로드 (기존 방식)
const allQnA = await loadQnADatabase();
```

**장점:**
- 초기 로딩 속도 향상
- 메모리 사용량 감소
- 특정 분야만 필요한 페이지에서 유용

---

## 📈 예상 효과

### 1. 관리 편의성 증가
- CSS 전문가는 `css.json`만 관리
- Git 전문가는 `git.json`만 관리
- 역할 분담이 명확해짐

### 2. 협업 효율성 향상
- 서로 다른 카테고리를 수정하면 merge conflict 없음
- Pull Request 리뷰가 더 명확해짐

### 3. 검색 및 분석 용이
- 카테고리별 통계를 한눈에 확인
- 어떤 분야의 Q&A가 부족한지 파악 가능
- 조회수 기반으로 인기 분야 분석

### 4. 확장성
- 새로운 카테고리 추가 시 파일만 추가하면 됨
- 각 카테고리가 커져도 다른 카테고리에 영향 없음

---

## 🎁 보너스: 통계 대시보드 컴포넌트 (향후 구현 예정)

카테고리별 통계를 시각화하는 관리자 페이지:

```jsx
// app/admin/stats/page.jsx (예시)

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card"

export default async function StatsPage() {
  const stats = await fetch('/data/categories/stats.json').then(r => r.json());

  return (
    <div className="grid gap-4 md:grid-cols-3">
      {Object.entries(stats.categories).map(([category, data]) => (
        <Card key={category}>
          <CardHeader>
            <CardTitle>{category}</CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold">{data.count}개</div>
            <p className="text-sm text-muted-foreground">
              총 {data.views}회 조회
            </p>
          </CardContent>
        </Card>
      ))}
    </div>
  );
}
```

---

## 📝 체크리스트

### 완료된 작업
- [x] public/data/categories/ 폴더 생성
- [x] 카테고리별 JSON 파일 분리 (6개 파일)
- [x] stats.json 통계 파일 생성
- [x] README.md 사용 가이드 작성
- [x] 기존 qna-database.json 유지 (하위 호환성)

### 향후 작업 (선택)
- [ ] 자동화 스크립트 작성 (merge, stats 생성)
- [ ] 검증 스크립트 작성 (중복 체크, 필수 필드)
- [ ] 카테고리별 선택적 로딩 구현
- [ ] 관리자 대시보드 페이지 추가

---

## 💬 사용 예시

### 예시 1: CSS Q&A만 확인
```bash
# public/data/categories/css.json 파일 열기
cat public/data/categories/css.json | jq '.qnaList[] | .question'
```

**출력:**
```
"flexbox에서 justify-content와 align-items 차이가 헷갈려요"
"position: absolute를 사용하면 요소가 사라져요"
```

### 예시 2: 전체 통계 확인
```bash
# public/data/categories/stats.json 파일 열기
cat public/data/categories/stats.json | jq '.categories'
```

**출력:**
```json
{
  "Frontend": { "count": 1, "views": 87 },
  "Backend": { "count": 1, "views": 121 },
  "CSS": { "count": 2, "views": 290 },
  "JavaScript": { "count": 1, "views": 93 },
  "Git": { "count": 1, "views": 67 },
  "도구": { "count": 2, "views": 120 }
}
```

---

## 🎯 결론

카테고리별 데이터 분리를 통해:

1. **관리 편의성** ↑: 각 분야별로 독립적인 파일 관리
2. **협업 효율성** ↑: merge conflict 감소
3. **확장성** ↑: 새 카테고리 추가 용이
4. **성능** ↑: 향후 선택적 로딩 시 초기 로딩 속도 개선
5. **분석** ↑: 카테고리별 통계로 데이터 분석 가능

데이터가 계속 쌓여도 체계적으로 관리할 수 있는 구조가 완성되었습니다! 🚀

---

## 📂 파일 위치

모든 카테고리별 데이터와 가이드는 다음 위치에 있습니다:
```
public/data/categories/
```

자세한 사용법은 `public/data/categories/README.md`를 참고하세요.
